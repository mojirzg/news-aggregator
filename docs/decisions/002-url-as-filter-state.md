# ADR 002 — URL as public filter state

**Status:** Accepted

Keyword, source, category, and date filters live in the URL. This gives shareable searches, reload persistence, deterministic query keys, and correct browser back/forward behavior without a global state store.
