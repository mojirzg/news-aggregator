# ADR 003 — Local storage preferences

**Status:** Accepted

The assessment requires personalization but does not require authentication. Preferences therefore use a versioned, runtime-validated local-storage schema. Server persistence would add an identity system outside the requested scope.
