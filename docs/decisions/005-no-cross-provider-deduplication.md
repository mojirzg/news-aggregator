# ADR 005 — No cross-provider deduplication

**Status:** Accepted

Semantic deduplication, story clustering, and canonical story identity require fuzzy matching, ranking, and product rules that exceed this frontend assessment. Exact or naive title matching would create false positives. The limitation is documented rather than hidden.
